/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},  // Opcional: Para añadir soporte a prefijos de CSS automáticamente
  },
};

export default config;
